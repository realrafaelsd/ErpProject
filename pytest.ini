[pytest]
addopts = -m "not real_models and not browser"
testpaths = tests
markers =
    real_models: opt-in tests that require locally installed embedding and generation models
    browser: opt-in tests that require locally installed Playwright browsers
